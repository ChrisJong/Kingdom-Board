namespace Unit {

    using UnityEngine;

    using Constants;
    using Enum;
    using Helpers;
    using UI;
    using System;

    [RequireComponent(typeof(LongbowUI))]
    public sealed class Longbow : UnitBase {
    }
}