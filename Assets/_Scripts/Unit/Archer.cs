namespace KingdomBoard.Unit {

    using UnityEngine;

    using Constants;
    using Enum;
    using Helpers;
    using UI;

    [RequireComponent(typeof(ArcherUI))]
    public sealed class Archer : UnitBase {
    }
}